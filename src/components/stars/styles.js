import {Colors, Constants} from '../../utils';

const styles = {
    container:{
        paddingHorizontal: 10,
        flexDirection: 'row',
    },
    star:{
        width:10,
        height:10,
        resizeMode:'contain',
        marginHorizontal: 1,
    }
}
export default styles;